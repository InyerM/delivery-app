export enum UserStatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
  PENDING = "pending",
  BANNED = "banned",
  DELETED = "deleted",
}

export enum UserTypeEnum {
  ADMIN = "admin",
  EATER = "eater",
  RESTAURANT = "restaurant",
  DRIVER = "driver",
}
